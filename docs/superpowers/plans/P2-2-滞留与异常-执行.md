# P2-2 滞留与异常 · 逐步 TDD 执行计划

> 展开日期：2026-06-18  
> 来源：`P2-2-滞留与异常.md`、`全周期实现计划总表.md`、`设计方案-整合版.md`。  
> 原则：垂直切片推进；每个 Task 先红灯测试，再最小实现，绿灯后提交。新增表必须带 `tenant_id` + RLS + `FORCE ROW LEVEL SECURITY`；前端新增页面接入现有主题、菜单、权限。

## 0. 当前基线与落位约定

- P1/P2-1 已完成：JWT/RBAC、RLS、`TenantPrismaService.withTenant`、EventBus、Redis、station/parcel/inbound/pickup/notify、shipping/pay/logistics、station-web/user-app。
- 后端目录使用当前仓库实际结构：`backend/apps/api/src/{core,modules/<context>}`。
- `bullmq` 与 `ioredis` 已在 `backend/package.json`；`core/queue/queue.module.ts` 目前只有骨架，需扩展为队列基础设施。
- `RedisLockService` 已实现 `SET NX PX` + Lua release，本期复用为 ShedLock 等价锁，不再新增平行锁实现。
- `parcel` 状态机已支持 `STORED -> EXCEPTION/RETURNED` 与 `EXCEPTION -> STORED/RETURNED`；本期补业务动作、事件、审计字段与 e2e。
- 本期新增权限码：`exception:create`、`exception:read`、`exception:handle`、`parcel:overdue:scan`。需同步 seed、auth 菜单与 station-web 权限常量。
- 本期新增/扩展表：
  - 新增 `exceptions`：异常工单，必须 RLS + FORCE。
  - 扩展 `parcels`：`last_overdue_level`、`overdue_returned_at`。
  - 可选扩展 `Station.overdue_config`（本期先默认常量；若 Task 2 需要配置层再加）。
- 本期接口统一在 `/api` 前缀下：
  - `POST /parcels/:id/exception`
  - `GET /exceptions`
  - `GET /exceptions/:id`
  - `POST /exceptions/:id/claim`
  - `POST /exceptions/:id/resolve`
  - `GET /parcels/overdue`
  - `POST /parcels/overdue/scan`

## Task 1 · BullMQ 队列基础设施与分布式锁适配

**Files**
- Extend: `backend/apps/api/src/core/queue/queue.module.ts`
- Create: `backend/apps/api/src/core/queue/queue.constants.ts`
- Create: `backend/apps/api/src/core/queue/repeatable.registrar.ts`
- Test: `backend/apps/api/src/core/queue/queue.module.spec.ts`
- Test: `backend/apps/api/src/core/redis/redis-lock.service.spec.ts`

**TDD**
1. RED: 队列模块测试断言可注入 BullMQ `Queue`，队列名为 `overdue-scan`。
2. RED: repeatable registrar 测试断言使用固定 `jobId=overdue-scan` 注册 repeatable job，重复注册不会产生多个配置。
3. RED: 锁测试补双实例并发 `acquire`，断言同 key 同时只有一个成功，release 后可再次获取。
4. GREEN: 注册 `QueueModule` providers，封装 `OVERDUE_SCAN_QUEUE` token 与 `RepeatableRegistrar`。
5. GREEN: 复用 `RedisLockService`，补必要测试，不新增第二套锁接口。
6. VERIFY: `npm test -- --runInBand queue redis-lock`。
7. COMMIT: `feat(backend): add overdue queue infrastructure`

## Task 2 · 滞留分级规则纯函数

**Files**
- Create: `backend/apps/api/src/modules/parcel/overdue/overdue-policy.ts`
- Test: `backend/apps/api/src/modules/parcel/overdue/overdue-policy.spec.ts`

**TDD**
1. RED: 边界测试断言 2 天为 0、3 天为 1、6 天为 1、7 天为 2、10 天为 2、11 天为 3、14 天为 3、15 天为 RETURN。
2. RED: 配置覆盖测试断言自定义 `remindDays/urgeDays/finalDays/returnDays` 生效。
3. RED: 非法配置测试断言阈值非递增时抛业务错误。
4. GREEN: 实现 `classifyOverdue(storedAt, now, cfg)` 与 `normalizeOverdueConfig`。
5. VERIFY: overdue policy 单测通过。
6. COMMIT: `feat(backend): add overdue policy rules`

## Task 3 · Parcel 异常/归位/退回动作

**Files**
- Extend: `backend/apps/api/src/modules/parcel/parcel.service.ts`
- Extend: `backend/apps/api/src/modules/parcel/parcel.aggregate.ts`
- Test: `backend/apps/api/src/modules/parcel/parcel.service.spec.ts`
- Modify: `backend/apps/api/src/core/http/api-code.ts`

**TDD**
1. RED: 服务测试断言 `markException` 仅允许 `STORED -> EXCEPTION`，写 `parcel_events` 并发布 `ParcelMarkedException`。
2. RED: 服务测试断言 `restock` 仅允许 `EXCEPTION -> STORED`，清理/重置 `lastOverdueLevel`。
3. RED: 服务测试断言 `returnParcel` 允许 `STORED/EXCEPTION -> RETURNED`，写 `overdueReturnedAt` 与 `ParcelReturned`。
4. RED: 非法状态测试断言 `PICKED_UP` 标异常/退回失败。
5. GREEN: 实现三个业务动作，使用乐观锁版本与现有 `ParcelAggregate.assertTransit`。
6. VERIFY: parcel service 单测通过。
7. COMMIT: `feat(backend): add parcel exception transitions`

## Task 4 · Prisma 模型、迁移与 seed 权限

**Files**
- Modify: `backend/prisma/schema.prisma`
- Create: `backend/prisma/migrations/<ts>_exceptions_overdue/migration.sql`
- Modify: `backend/prisma/seed.ts`
- Modify: `backend/apps/api/src/modules/identity/auth.service.ts`
- Test: `backend/test/exception-rls.e2e-spec.ts`

**TDD**
1. RED: RLS e2e 断言设置租户 A 后只能看到 A 的 `exceptions`，租户 B 数据不可见。
2. RED: seed/权限 e2e 断言店长拥有 `exception:create/read/handle` 与 `parcel:overdue:scan` 按设计返回。
3. GREEN: 新增 Prisma enum/model：`ExceptionType`、`ExceptionStatus`、`ExceptionResolution`、`Severity`、`ExceptionTicket`（表名 `exceptions`）。
4. GREEN: 扩展 parcel 字段 `lastOverdueLevel`、`overdueReturnedAt`，迁移中补 RLS/FORCE/索引/唯一约束。
5. GREEN: seed 默认权限和菜单，auth 菜单解除异常件禁用态并挂 `exception:read`。
6. VERIFY: `npx prisma generate`、RLS e2e、foundation e2e 相关断言通过。
7. COMMIT: `feat(backend): add exception models and permissions`

## Task 5 · 滞留扫描 Processor

**Files**
- Create: `backend/apps/api/src/modules/parcel/overdue/overdue-scan.processor.ts`
- Extend: `backend/apps/api/src/modules/parcel/parcel.module.ts`
- Test: `backend/apps/api/src/modules/parcel/overdue/overdue-scan.processor.spec.ts`

**TDD**
1. RED: 构造多个 STORED 包裹，断言扫描按 storedAt 分级并只升级 `lastOverdueLevel`。
2. RED: 断言重复扫描同级别不重复发布 `ParcelOverdueDetected`。
3. RED: 断言 `>= returnDays` 的包裹调用 `returnParcel(cause=OVERDUE)` 并发布 `ParcelReturned`。
4. RED: 断言锁未获取时直接返回 skipped，不处理任何包裹。
5. GREEN: 实现 `runOverdueScan(now?)`，平台 bypass 分页扫描，按 tenant/station 处理。
6. VERIFY: overdue scan 单测通过。
7. COMMIT: `feat(backend): scan overdue parcels`

## Task 6 · 分级催取通知订阅

**Files**
- Create: `backend/apps/api/src/modules/notify/overdue.subscriber.ts`
- Extend: `backend/apps/api/src/modules/notify/notify.module.ts`
- Extend: `backend/prisma/seed.ts`
- Test: `backend/apps/api/src/modules/notify/overdue.subscriber.spec.ts`

**TDD**
1. RED: 订阅测试断言 level 1/2/3 分别使用 `OVERDUE_REMIND/OVERDUE_URGE/OVERDUE_FINAL` 模板。
2. RED: 去重测试断言同 parcel+level 重复事件不重复通知。
3. RED: 租户上下文测试断言通知落在事件 tenantId 下。
4. GREEN: seed 三级模板，订阅 `ParcelOverdueDetected` 并调用现有 NotifyService。
5. VERIFY: notify 单测通过。
6. COMMIT: `feat(backend): notify overdue parcels`

## Task 7 · Exception 工单状态机与服务

**Files**
- Create: `backend/apps/api/src/modules/exceptions/exception.aggregate.ts`
- Create: `backend/apps/api/src/modules/exceptions/exception.service.ts`
- Create: `backend/apps/api/src/modules/exceptions/exception.module.ts`
- Test: `backend/apps/api/src/modules/exceptions/exception.aggregate.spec.ts`
- Test: `backend/apps/api/src/modules/exceptions/exception.service.spec.ts`

**TDD**
1. RED: 状态机测试断言 `OPEN -> IN_PROGRESS -> RESOLVED` 合法，跳步/重复处理非法。
2. RED: 服务测试断言有 parcel 建单会调用 `parcel.markException`，同 parcel 未结工单会被拦。
3. RED: 服务测试断言 claim 落 `assigneeId`，仅 OPEN 可认领。
4. RED: 服务测试断言 resolve RESTOCK 调 `parcel.restock`，RETURN/VOID 调 `parcel.returnParcel`。
5. RED: 服务测试断言无主件可建单并 resolve VOID，不访问 parcel。
6. GREEN: 实现工单号生成、列表查询、详情、认领、解决与领域事件 `ParcelMarkedException` 的 payload 衔接。
7. VERIFY: exception aggregate/service 单测通过。
8. COMMIT: `feat(backend): add exception ticket service`

## Task 8 · 异常与滞留 REST API

**Files**
- Create: `backend/apps/api/src/modules/exceptions/exception.controller.ts`
- Extend: `backend/apps/api/src/modules/parcel/parcel.controller.ts`
- Extend: `backend/apps/api/src/app.module.ts`
- Test: `backend/test/exception-api.e2e-spec.ts`
- Test: `backend/test/overdue-api.e2e-spec.ts`

**TDD**
1. RED: e2e 断言 `POST /api/parcels/:id/exception` 建工单并使 parcel 进入 EXCEPTION。
2. RED: e2e 断言 `GET /api/exceptions` 支持 status/type/keyword/page/size，并跨租户隔离。
3. RED: e2e 断言 claim/resolve RESTOCK 后 parcel 回 STORED，RETURN 后 parcel RETURNED 且库位释放。
4. RED: e2e 断言 `GET /api/parcels/overdue` 返回分级列表，`POST /api/parcels/overdue/scan` 可手动触发。
5. RED: e2e 断言无 `exception:*` 或 `parcel:overdue:scan` 权限返回业务码 1003。
6. GREEN: 实现 controller、DTO 校验、权限守卫、overdue 查询与手动扫描入口。
7. VERIFY: exception/overdue e2e 通过。
8. COMMIT: `feat(backend): expose exception and overdue APIs`

## Task 9 · station-web API、权限与路由

**Files**
- Create: `station-web/src/api/exceptions.ts`
- Create: `station-web/src/api/overdue.ts`
- Test: `station-web/src/api/exceptions.spec.ts`
- Test: `station-web/src/constants/routes.spec.ts`
- Extend: `station-web/src/constants/perms.ts`
- Extend: `station-web/src/constants/routes.ts`

**TDD**
1. RED: API 测试断言异常列表/详情/建单/认领/解决参数映射正确。
2. RED: API 测试断言滞留查询/手动扫描 query 与响应映射正确。
3. RED: 路由测试断言 `/exceptions` 仅 `exception:read` 可见且不再指向占位页。
4. GREEN: 实现 API、权限常量和路由占位。
5. VERIFY: `npm test`。
6. COMMIT: `feat(station-web): add exception api and navigation`

## Task 10 · station-web 异常工作台与滞留看板

**Files**
- Create: `station-web/src/views/ExceptionsView.vue`
- Extend: `station-web/src/views/ParcelsView.vue`
- Extend: `station-web/src/styles/app.css`
- Test: `station-web/src/constants/routes.spec.ts` 或轻量 helper 测试

**TDD**
1. RED: 轻量测试断言异常路由使用真实视图。
2. RED: helper 测试断言异常类型/状态/resolution 标签稳定。
3. GREEN: 实现异常列表、筛选、认领、处理弹窗、滞留看板、手动扫描按钮。
4. GREEN: 在包裹列表行增加「标记异常」入口（受 `exception:create` 控制）。
5. VERIFY: `npm test && npm run build`，页面手动完成标记异常 -> 认领 -> RESTOCK/RETURN。
6. COMMIT: `feat(station-web): add exception workspace`

## Task 11 · P2-2 后端闭环 e2e 与验收

**Files**
- Create: `backend/test/overdue-exception.e2e-spec.ts`
- Modify: `docs/superpowers/plans/00-总控计划.md`
- Modify: `docs/superpowers/plans/P2-2-滞留与异常-执行.md`

**TDD / 验收**
1. RED: e2e 造 3/7/11/15 天 STORED 包裹，跑 scan 后断言分级通知、超期 RETURNED、库位释放。
2. RED: e2e 造异常包裹，断言 mark -> claim -> RESTOCK 回 STORED；再造一单 mark -> claim -> RETURN 到 RETURNED。
3. GREEN: 补齐扫描、事件、通知、工单边界。
4. VERIFY: `backend npm run format && npm test -- --runInBand && npm run test:e2e -- --runInBand && npm run build`。
5. VERIFY: `station-web npm test && npm run build`，手动点通异常与滞留流程。
6. 更新总控：勾选 P2-2，记录完成日期、最后实现提交与验收摘要。
7. COMMIT: `docs(plan): 标记 P2-2 滞留与异常完成`

## 完成记录

- 完成日期：2026-06-18
- 最后实现提交：`b7bee32 feat(station-web): add exception workspace`
- 闭环验收提交：`bf0a684 test(backend): add overdue exception closure e2e`
- 核心交付：
  - 后端：BullMQ 滞留扫描基础设施、Redis 分布式锁、滞留分级规则、`exceptions` 表 + RLS/FORCE、异常工单服务与 REST API、分级催取通知订阅、超期自动退回并释放库位。
  - station-web：异常/滞留 API、权限与路由、异常工作台、滞留看板、包裹列表标记异常入口。
  - 验收：3/7/11/15 天 STORED 包裹扫描后分级通知、15 天退回释放库位；异常工单 mark → claim → RESTOCK 回 STORED、mark → claim → RETURN 到 RETURNED。
- 验证命令：
  - `backend npm run format && npm test -- --runInBand`：29 suites / 109 tests 通过。
  - `backend npm run test:e2e -- --runInBand && npm run build`：18 suites / 25 e2e tests 通过，构建通过。
  - `station-web npm test && npm run build`：13 files / 29 tests 通过，构建通过；构建仅保留既有 chunk size / Rollup pure 注释提示。
