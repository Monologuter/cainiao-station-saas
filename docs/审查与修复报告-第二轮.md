# 菜鸟驿站 SaaS · 第二轮审查与修复报告（交给 Codex 执行）

> 日期 2026-06-18 ｜ 范围：后端/前端/ai-service/数据模型/DevOps/功能完整性全维度复审（只读审查，未改代码）。
> 用法：把本文件交给 Codex，按「优先级批次」逐条修复，每条都给了**位置 / 问题 / 修法 / 验收**。每修一批跑测试+冒烟再提交。

## 0. 背景与基调

- 核心收发闭环**真能用、能跑起来**（已实测：后端启动→开店→登录→入库→通知→取件→库位释放、前端登录进工作台渲染真实数据）。
- 但本轮发现：**多处"文档标完成"实为演示级桩/断链**，以及成片的**安全加固缺口**和**部署/测试工程短板**。
- **上轮已修复、请勿重复处理**：跨租户读写、取件零验证、通知改 BullMQ、入库原子性+P2002、ShipOrder 乐观锁、定价除零、门店 scope 强制收敛、/auth/me 契约、v-perm 恢复、refreshToken（station/admin）、user-app 暗色主题、AssistantClient 超时熔断、deploy/+MinIO、文档登记 4 上下文。

## 1. 优先级与建议批次

| 批次 | 主题 | 包含 ID |
|---|---|---|
| **P0-A 安全（必须先修）** | 账号接管/伪造/泄露 | SEC-1 SEC-2 SEC-3 SEC-4 SEC-5 SEC-6 SEC-7 SEC-8 |
| **P0-B 数据正确性** | 隔离/唯一/补偿 | DM-1 DM-2 BE-1 BE-2 BE-3 SEC-9 SEC-10 |
| **P1 功能断链（标完成实为桩）** | 让"已完成"真能用 | FUNC-1 FUNC-2 FUNC-3 FUNC-4 FUNC-5 FUNC-6 FUNC-7 BE-4 |
| **P2 部署与测试工程** | 可部署/可信测试 | OPS-1 OPS-2 OPS-3 OPS-4 OPS-6 |
| **P3 质量/性能/一致性** | 优化 | BE-5…BE-14 / FE-* / DM-3…7 / SEC-11…14 / OPS-5/7 |

---

## 2. 安全（SEC）

| ID | 严重度 | 位置 | 问题 | 修法 | 验收 |
|---|---|---|---|---|---|
| SEC-1 | 🔴阻断 | `member/member.service.ts:8,24-36` | 消费者登录硬编码固定验证码 `'123456'`，`@Public` 路径任意手机号+123456 即签 7 天令牌→**全账号接管**（叠加 SEC-6 可枚举他店） | 真 OTP：随机码存 Redis(TTL300s)+限发限验+单次使用；mock 仅 `NODE_ENV!=='production'` 开 | 生产下 123456 不通过；仅新生成单次码可验 |
| SEC-2 | 🔴重大 | `core/rate-limit/rate-limit.service.ts:23`；`member/member.controller.ts:32-40` | 限流是**单机内存 Map**（多实例/重启即失效，轮询可绕过）；OTP send-code/verify **无限流**（短信轰炸+撞码） | 限流改 Redis 滑窗/令牌桶；OTP 端点加 `@RateLimit`(按 ip+phone) | 双实例压测总放行=阈值；同号 60s 内二次 send-code 返回 429 |
| SEC-3 | 🔴重大 | `identity.module.ts:12`、`jwt.strategy.ts:12`、`member.module.ts:21`、`review.module.ts:18`、`analytics.module.ts:33`；`ai/assistant.client.ts:34` | JWT 密钥 `?? 'dev-secret-change-me'` 5 处 fail-open + AI 服务令牌 `?? 'dev-service-token'`，缺 env 用公开常量→可伪造平台 JWT | 去掉 `??` 兜底，ConfigModule validationSchema 缺失即启动失败；密钥单一来源 | 不设 JWT_SECRET 启动报错；旧默认密钥签的 token 被拒 |
| SEC-4 | 🔴重大 | `identity/jwt.strategy.ts:16-66`；`auth.service.ts:251,290` | access token 无吊销/版本号；**停用用户/改密后旧 access 在 TTL(2h) 内仍有效**（revokeSessions 只清 refresh，strategy 不校验 user 状态） | validate 内校验 user `disabled/deletedAt` + `tokenVersion`（改密/停用自增，payload 带版本比对）；或 access TTL 降至 5-15min | 停用用户旧 token 立即 401；改密后旧 access 失效 |
| SEC-5 | 🔴重大 | `main.ts:1-15` | 无 Helmet、无显式 CORS、无安全响应头(HSTS/CSP/X-Frame) | `app.use(helmet())` + `enableCors({origin:白名单,credentials})` | 响应含安全头；跨域仅白名单 |
| SEC-6 | 🔴重大 | `member/member.controller.ts:84-91`(@Public)；`point.service.ts:150-176` | F3：积分榜 tenantId 取自 query 未校验归属→枚举任意门店 top10(memberId+分值) | tenantId 由上下文/归属推导，不取 query；memberId 脱敏 | 传他店 tenantId 返回空/403；不暴露原始 memberId |
| SEC-7 | 🔴重大 | `shipping.controller.ts:60`(`@Body() any`)、`billing/plan/plan.controller.ts:17`、`member.controller.ts:155`、`exceptions/exception.controller.ts:11,42`、`config/dictionary.controller.ts:60,76,121…`(TS `type` 非 class) | 寄件下单/套餐价/券面值/异常枚举/系统配置等用 `any` 或 TS type，ValidationPipe 无 metadata→未校验直入库（负数/NaN/任意枚举） | 全改带 `@IsInt/@Min/@IsEnum/@IsNumber` 的 class DTO；开 `forbidNonWhitelisted` | 非法金额/枚举被拒；消费者寄件复用 CreateShipOrderDto |
| SEC-8 | 🔴重大(真接前必换) | `pay/wechat-pay.channel.ts:209-234`；`pay.controller.ts:10` | 微信支付验签是 HMAC-SHA256 占位（真 V3 为平台证书 RSA 验签+AES-GCM 解密）；且 `===` 非常量时间比较；回调 body 为 any | 实现 V3 证书验签+资源解密；比较用 `crypto.timingSafeEqual`；回调 DTO 校验 | 构造 V3 回调验签通过/失败符合预期；错误签名拒绝 |
| SEC-9 | 🟠一般(潜伏阻断) | RLS 迁移 `20260617175237_rls`（仅 Station/User） | `Role/UserRole/RolePermission` 带租户数据**无 RLS**，`Role.tenantId` 可空；一旦上线租户角色管理端点即越权/提权通道 | 建端点**前**先给 RBAC 表加 RLS（仿 Station/User）；端点 tenantId 钉死 JWT，拒跨租户 roleId、限可授权限白名单 | 租户管理员无法造/分配跨租户或平台角色，DB 层也拒 |
| SEC-10 | 🟠一般 | RLS 迁移（25/32 表仅 USING 无 WITH CHECK） | 写策略缺显式 WITH CHECK，靠隐式兜底；日后非对称 USING 会意外放开写 | 为业务表补对称 `WITH CHECK`（模板见 `20260618233000_wechat...`） | 错误 tenant_id 会话 INSERT 跨租户行被 DB 拒 |
| SEC-11 | 🟠一般(业务确认) | `pickup/pickup.controller.ts:7-22`、`pickup.service.ts:41-78` | 取件"任一因子"，取件码非必填，单凭 parcelId/手机尾号可核销→冒领 | 核销强制 `pickupCode` + `phoneTail` 双因子；parcelId 仅定位 | 缺码或尾号不匹配拒核销 |
| SEC-12 | 🟠一般 | `permission.guard.ts:19`、`tenant-prisma.service.ts:12` | `isPlatform` 同时绕过权限+RLS（双保险全失） | 确保 `type=PLATFORM` 永不可由租户侧赋予 + 平台操作全审计；危险操作即便平台也要显式 perm | 平台账号操作有审计；置位路径受控 |
| SEC-13 | 🟠一般 | `auth.controller.ts:11`、`tenant.controller.ts:16`；`.env.example`/`docker-compose.yml` | 口令仅 `@MinLength(6)`、临时密码不强制改；.env 弱默认凭据(cainiao/cainiao 等) | 提至≥8(员工≥12)+复杂度+@MaxLength；临时密码首登强制改；.env.example 改占位 `CHANGE_ME` | 弱口令被拒；首登改密 |
| SEC-14 | 🟢优化 | `circuit-breaker.service.ts`(未被出站调用包裹)；`station.controller.ts:42`(slot 分页无上限) | 熔断器未接支付/物流/短信出站调用；slot listFree 分页 `?limit=1000000` 照收 | 出站调用包熔断；分页 `Math.min(limit,100)` | 下游故障快速失败；分页有封顶 |

---

## 3. 数据模型与迁移（DM）

| ID | 严重度 | 位置 | 问题 | 修法 | 验收 |
|---|---|---|---|---|---|
| DM-1 | 🔴重大 | `schema.prisma`(无 StaffStation)；`auth.service.ts:286`(assignedStationIds 返 `[]`)；`station-scope.ts:69` | **staff_stations 分配表缺失**→店员可见门店恒空→门店级列表/查询对店员一律 403（安全兜底但功能不可用） | 建 `StaffStation(tenantId,userId,stationId @@unique([userId,stationId]) + RLS)`；`assignedStationIds` 改查该表；写路径按集合校验 | 分配门店后店员只见该门店、跨门店 403；撤销后不可见 |
| DM-2 | 🔴重大 | `migrations/...parcel_status_events/migration.sql:46`（parcels 无 waybill 唯一） | 并发首扫可重复建包裹占双库位 | 加部分唯一索引 `ux_parcel_active_waybill ON parcels(tenant_id,station_id,waybill_no) WHERE deleted_at IS NULL AND status IN('PENDING','STORED','EXCEPTION')`；入库 upsert 幂等 | 并发同 waybill 仅 1 条在库，第二条走幂等 |
| DM-3 | 🟠一般 | `shelves/slots/exceptions/ship_orders/...` 的 `@@unique` | 自然键全表唯一(不含 deleted_at)→软删后无法用同 code 重建 | 改 `WHERE deleted_at IS NULL` 部分唯一索引 | 软删后可同 code 重建，活跃集仍唯一 |
| DM-4 | 🟠一般 | `prisma/seed.ts`(faqEntry/role/user/notifyTemplate/priceRule 裸 create) | seed 非幂等，二次跑报错/重复 | 改 upsert / createMany(skipDuplicates) | 连跑两次 seed 不报错无重复 |
| DM-5 | 🟠一般 | `migrations/20260618170000_billing_models/migration.sql:2` | `ALTER TYPE ADD VALUE` 无 `IF NOT EXISTS`→重放报错 | 统一加 `IF NOT EXISTS`；如需即用拆独立迁移 | 已含该枚举值的库重放迁移不报错 |
| DM-6 | 🟠一般 | `schema.prisma:1240`(ShipOrder.consumerId 无 @db.Uuid) | 类型漂移(text vs uuid)，索引类型不匹配 | 改 `@db.Uuid` + 迁移 `ALTER COLUMN ... TYPE uuid USING ...` | 列为 uuid，listener 查询走 uuid 索引 |
| DM-7 | 🟢优化 | `ship_orders/logistics_tracks @@index([waybillNo])`、`point_records([sourceTenantId])` | 索引缺 tenant_id 前导；平台级表(applications/audit)对租户零读路径需复核 | 补 tenant_id 前导联合索引；复核平台级表权限 403 | EXPLAIN 走索引；租户 token 访问平台表 403 |

---

## 4. 后端质量与性能（BE）

| ID | 严重度 | 位置 | 问题 | 修法 | 验收 |
|---|---|---|---|---|---|
| BE-1 | 🔴重大 | `exceptions/exception.service.ts:158-182` | 异常工单 resolve **跨多事务无锁**，update where 仅 `{id}`→并发可重复补偿(退货/补库 2 次) | 整流程包进一个 `$transaction`，补偿用同 tx；工单 update where 加 `status` 用 updateMany count===1 | 并发 resolve 仅一方成功，无双补偿 |
| BE-2 | 🔴重大 | `billing/jobs/invoice-run.processor.ts:50-66` | 批处理无 try/catch→一户开票失败中断整批 | 循环体逐户 try/catch + 记失败 + continue（保持每户独立事务） | 注入一户失败，其余仍出账，结果含失败明细 |
| BE-3 | 🔴重大 | `config/runtime-config.service.ts:7-34` | 进程内 Map 缓存无 TTL、invalidate 只清本进程→多实例脏读无限期 | 接 `MultiLevelCacheService`(TTL) 或 Redis pub/sub 广播失效 | 改配置后所有实例在 TTL 内收敛/即时失效 |
| BE-4 | 🔴重大 | `billing/subscription/subscription.service.ts:114-132` | 混合计费缺月中 proration；changePlan 无状态守卫(可对已取消订阅换套餐) | 月中换套餐按日单价算补/退差额生成调整账单；加状态守卫(仅 ACTIVE/TRIALING) | 升档补差/降档抵扣；换套餐有状态校验 |
| BE-5 | 🟠一般 | `admin/monitor/monitor.service.ts:49,74` | 门店监控 N×4 查询(每门店 4 次) | groupBy/批量聚合降到恒定条数 | 查询数与门店数解耦 |
| BE-6 | 🟠一般 | `identity/jwt.strategy.ts:16-38` | 每鉴权请求 2 次查库算权限 | 按角色集 Redis 短 TTL 缓存(角色变更失效) | 高频鉴权命中缓存，DB QPS 降 |
| BE-7 | 🟠一般 | `coupon.service.ts:74,84`、`member.service.ts:129`、`invoice/usage/subscription list` | 伪分页(无 take/skip，全表进内存) | 加 take/skip，total 用单独 count | 各 list 受 page/size 限制 |
| BE-8 | 🟠一般 | parcels 缺 `(tenant_id,waybill_no)`；consumer 查件 `receiverPhone` 用不上复合索引 | 查件全表扫 + 无分页 | 加 `(tenant_id,waybill_no)`、`(receiver_phone,created_at)` 索引 + 分页 | 查件走索引、单页有上限 |
| BE-9 | 🟠一般 | `member/coupon.service.ts:96-145` | 积分兑换券跨事务：扣分与发券不原子，失败则扣分无券且幂等键占用补不回 | 扣分+发券合并单事务(幂等前置) | 同成同败，重试最终有券 |
| BE-10 | 🟠一般 | `pay/wechat-pay.channel.ts:134`、`analytics.controller.ts:172,180`、`analytics.gateway.ts:103` | 裸 `new Error`/`BadRequestException`→未分类 500/契约不一致 | 统一 BizError+ApiCode；WS 断连前记日志 | 返回统一 `{code,message}`，无裸 500 |
| BE-11 | 🟠一般 | parsePositiveInt(×4)、addMonths(×3)、脱敏 slice(-4)(×5)、分页偏移(×7) | 工具函数多处复制、脱敏格式不统一 | 抽 `core/{pagination,date,privacy}` 公共工具 | 各模块引用公共实现，行为不变 |
| BE-12 | 🟠一般 | `parcel.service.ts`(720 行)；`:561` 逾期阈值硬编码与 `overdue-policy.ts` 双源 | God Service 多职责；阈值两处真相 | 拆 Query/State service；逾期统一调 classifyOverdue | 阈值单源；职责收敛 |
| BE-13 | 🟢优化 | `logistics.service.ts:117-166` | GET getTracks 内联写库(已加锁，仍读写耦合) | 物化/推进下沉回调/定时任务，读接口纯读 | 无新节点时 getTracks 不写库 |
| BE-14 | 🟢优化 | `multi-level-cache.service.ts:82`、`inbound.service.ts safeRelease` | 吞错无日志 | catch 补 `logger.warn` | 缓存/补偿失败可观测 |

---

## 5. 前端（FE）

| ID | 严重度 | 位置 | 问题 | 修法 | 验收 |
|---|---|---|---|---|---|
| FE-1 | 🔴重大 | `station-web/src/constants/routes.ts`(全静态 import)；`station/admin main.ts`(EP 全量) | station 单包 **1.2MB** 未拆分；EP 全量引入 | 路由 `() => import()` 懒加载 + `unplugin-vue-components`+ElementPlusResolver 按需 + manualChunks | 主包<500KB，vendor 独立 chunk |
| FE-2 | 🔴重大 | admin `BillingView/ApplicationsView/AuditView/PlansView/StoresMonitorView`；user-app `pages/parcels`+`stores/parcel` | 列表 `Promise.all` 只 try-finally 无 catch→失败静默；user 核心页无 loading | 加 catch→错误提示 + 空态；user 加 loading 骨架+toast | 断网三页有错误提示；空/失败有反馈 |
| FE-3 | 🔴重大 | `user-app/src/utils/request.ts:42-60` | 无 401/刷新拦截，token 过期直接失败(与另两端不一致) | 实现 uni 版 401 拦截+刷新重试，复用 station 刷新协议 | user-app token 过期能自动续期 |
| FE-4 | 🔴重大 | user-app 缺 `messages/authorize/profile`(高保真 9 页缺 3) | 消息通知/取件授权/个人中心三核心页缺失 | 新建 `pages/*`+pages.json 路由，对齐高保真 | 三页可访问且对齐高保真 |
| FE-5 | 🔴重大 | user-app `ship/complaints/login`(input 无 label)；admin `AuditView:152` 等(span 当按钮) | 无障碍：表单缺 label、span 点击不可键盘达 | 加 `<label>`/`aria-label`；span 改 `<button>` 或加 role+tabindex+keydown | 读屏可朗读；Tab/回车可达 |
| FE-6 | 🔴重大 | station `StatisticsView.vue:614,650`、`ShelvesView.vue:385` | 2 处图表硬编码色→切 dark/mint 不变色(三主题失效) | 改 `var(--primary)/var(--warn)/var(--danger)` | 三主题切换该区块颜色跟随 |
| FE-7 | 🟠一般 | admin `src/styles/app.css`(7 处硬编码)；三端各维护一份 token | token 未真正复用 kit | 抽共享 design-token 文件三端 import；admin 硬编码改 token | 改一处 primary 三端同步 |
| FE-8 | 🟠一般 | station `Complaints/Reviews/Coupons`(缺空态)；三端错误 toast 风格不一 | 体验不一致 | 补空态；请求层做统一全局错误 toast | 空列表有提示；错误风格统一 |
| FE-9 | 🟠一般 | admin `TenantsView/PlatformUsersView`(空骨架) | 依赖后端 /admin/* 未实现（见 FUNC-6） | 后端补接口后填充表格/操作 | 拉到真实列表、可操作 |
| FE-10 | 🟢优化 | station `PlaceholderView.vue`(死代码)、`OnboardingApplyView mockUpload`(误导命名)；三端 `ApiResponse/unwrap` 重复 | 死代码/命名/重复 | 删 PlaceholderView；mockUpload 改名；抽 `@shared/http-client` | 构建测试仍过；三端复用一份 |

---

## 6. DevOps / 测试工程（OPS）

| ID | 严重度 | 位置 | 问题 | 修法 | 验收 |
|---|---|---|---|---|---|
| OPS-1 | 🔴重大 | `backend/`、`ai-service/`(无 Dockerfile)；`deploy/docker-compose.yml:74` app profile | 全仓无 Dockerfile/.dockerignore→`--profile app` build 必败，"一键起整套"不可达 | 加 `backend/Dockerfile`(多阶段+prisma migrate deploy)、`ai-service/Dockerfile`、各 `.dockerignore`；compose 补 JWT_SECRET 注入 | `compose --profile app up --build` 三服务 healthy |
| OPS-2 | 🔴重大 | `test/*.e2e-spec.ts`(裸 `new PrismaService()`)、`jest-e2e.json`(无 maxWorkers)、`package.json test:e2e`(无 --runInBand) | e2e 并行+叠连接池+零 DB 清理→连接耗尽+onboarding flaky | `--runInBand`/maxWorkers:1；抽 `test/setup.ts` 单例 app+共享 prisma；globalSetup+truncate；进阶 per-worker schema | 连跑 3 次全绿无 too many clients、无 flaky |
| OPS-3 | 🔴重大 | 仓库根(无 `.github/workflows`) | 完全无 CI，无门禁 | 加 CI：services pg+redis→migrate→lint→test→test:e2e(串行)；ai-service pytest；三前端 build；`compose config -q` | PR 全 job 绿才可合 |
| OPS-4 | 🟠一般 | `deploy/docker-compose.yml:75-126`(应用服务无 healthcheck)；nginx depends_on 无 condition | api 未就绪 nginx 已上游→502 | api/ai-service 加 healthcheck(探 /health/ready、/readyz)；nginx `condition: service_healthy` | `compose ps` 显示 (healthy) |
| OPS-5 | 🟠一般 | `P3-4-工程化加固.md` vs 代码 | 文档把 pino/prom-client/Sentry 写成已落地，实际未落地(用默认 Logger/自实现指标/无 Sentry) | 要么补 pino/prom-client 落地，要么把文档降级表述对齐代码 | 文档与代码逐条一致 |
| OPS-6 | 🟠一般 | `backend/.env.example`(漏 AI_SERVICE_URL/OVERDUE_SCAN_CRON)；`ai-service/`(无 .env.example)；仓库根(无 README) | 代码已引用变量缺样例；ai-service 无 env 样例；无总入口 README | 补缺失 env 项 + `ai-service/.env.example`(ANTHROPIC_API_KEY 等) + 根 README(架构/端口表/一键起) | 照 README 干净机可起全套 |
| OPS-7 | 🟢优化 | 定时任务 Redis NX 锁(已到位) | 长任务超 TTL 后可能并行 | 长任务接看门狗续租；文档注明 worker 并发=1+锁假设 | 长任务锁不中途失效 |

> 说明：定时任务多实例防重(Redis NX+Lua 释放)、health/ready 探针、traceId(ALS)、自实现 Prometheus 指标——**这些是真代码且有测试**，质量好于文档预期，主要问题是文档失实(OPS-5)。

---

## 7. 功能完整性 — 文档标"完成"实为桩/断链（FUNC）

| ID | 严重度 | 位置 | 规格 vs 实际 | 修法 | 验收 |
|---|---|---|---|---|---|
| FUNC-1 | 🔴重大 | `station/slot-allocator.service.ts:174-175` | **智能库位推荐空转**：scorer 四因子算法真，但喂入 `pickCount7d:0`/`hourHistogram:Array(24).fill(0)`/`sizeClass:'M'` 写死；slot_heat_daily 采集了不回流→≈P1 顺序分配 | 把 slot_heat_daily/取件频率/包裹规格真数据回流进 recommendSlot | 推荐随历史热度变化，非恒定 fallback |
| FUNC-2 | 🔴重大 | billing jobs 未注册 repeatable registrar；`auth.service.login`/guard 不查 `tenant.status` | **欠费停用无牙**：①无定时触发器→永不自动出账/停用 ②suspended 租户照常登录操作 | 把 invoice-run/expiry-check 注册进定时调度；登录/守卫校验 tenant.status，suspended 拒绝业务写 | 到期自动出账；欠费 SUSPEND 后该租户被拦 |
| FUNC-3 | 🔴重大 | 三前端无 `socket.io-client`，`StatisticsView.vue:118` 15s 轮询；`report.processor.ts:24` buildCsv 丢弃、fileKey `mock://` | **实时大屏是轮询**(后端 Gateway 真但前端没连)；**报表导出是桩**(无真文件，下载不了) | 前端接 socket.io-client 订阅 Gateway；报表用 exceljs/csv 生成真文件入 file-storage | 大屏事件驱动实时刷新；报表能下载真文件 |
| FUNC-4 | 🔴重大 | `notify.service.ts`(写死 `['IN_APP','SMS']`)；催取/入驻通知进程内 await 不入队 | **微信订阅渠道孤儿**(能力建好从不调用)；**只有 ParcelStored 走队列**，催取/入驻无重试死信 | notify 出口注入并按场景择路微信 factory；所有通知统一入 BullMQ 队列 | 微信渠道可触发；各通知有重试/死信 |
| FUNC-5 | 🔴重大 | `pickup.service.ts`(无授权校验)；`pay/shipping` grep coupon 零命中；无 `POST /shipping/orders/:id/cancel`、无退款端点 | **代取授权僵尸表**(取件不校验授权)；**优惠券抵扣无人算**(verify 只置 USED)；**寄件取消/退款端点缺失**(refund primitive 无调用方) | 取件接 pickup_authorizations 校验；支付读券抵扣；补寄件取消+退款端点+每日对账 | 被授权人可代取；下单用券真减额；可取消并退款 |
| FUNC-6 | 🔴重大 | `tenant.controller.ts`(只有 POST 建租户)；无 platform-user 任何实现 | **admin tenants 列表/启停缺失**；**platform-users 完全缺失**(前端空骨架) | 后端补 `GET /platform/tenants`+`PATCH status`；补平台员工 CRUD+角色分配；前端填充 | 租户页拉真实列表可启停；平台用户可管理 |
| FUNC-7 | 🟠高 | `ai-service/app/llm/claude_llm.py`、`assistant.service.ts` | 大模型客服 real 路径**生产零接线**(`AI_ASSISTANT_MODE=real` 永久降级 FAQ)；mock 是回声模板 | 接线 claude_llm 生产注入(ANTHROPIC_API_KEY)，real 模式真走 LLM | 配 key 后 real 模式真调 Claude，降级仍回落 FAQ |
| FUNC-8 | 🟢优化 | `monitor.service.ts:117`(online 硬编码 true)；reconcile 无定时；积分加分硬编码散落 | 在线因子恒真；对账只手动；加分规则未集中 | online 真算；reconcile 加定时；积分规则集中配置 | 监控在线真实；对账自动；规则单源 |

---

## 8. 给 Codex 的执行提示词（可直接粘贴）

```text
读 docs/审查与修复报告-第二轮.md。按其「优先级批次」P0-A→P0-B→P1→P2→P3 逐批修复菜鸟驿站代码：
- 每条按表里的「修法」改，并补/改对应回归测试，满足「验收」列。
- 遵守锁定技术栈与既有约定(BizError/ApiCode/TenantPrismaService.withTenant/@RequirePermission/BullMQ/RLS)；新业务表必须加 tenant_id+RLS Policy+FORCE。
- 容器用 cainiao-station 编组与错开端口(PG15432/Redis16379/MinIO19000-1/后端3100)，不占用已有端口。
- 每修完一批：后端 npm test + 必要 e2e(串行) 绿、相关前端 build+test 绿，再 git commit(约定式 message)。每批结束停下来汇报。
不确定的业务取舍(如取件双因子 SEC-11、计费 proration BE-4)先提出建议再改。
```
